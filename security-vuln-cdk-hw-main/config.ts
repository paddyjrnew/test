import { Config } from "cdk-extensions";

/**
 * Allows you to specify configuration values switching on
 * account and environment. In general, you'll add any config
 * variables under the 'SettingsModule.client' namespace.
 * See https://github.com/rafastealth/cdk-extensions#configuration-variables
 */
export const config: Partial<Config> = {};
